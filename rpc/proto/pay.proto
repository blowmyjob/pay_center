syntax = "proto3";
// 定义包名
package rpc.proto;

// 定义服务
service PayService {
  rpc CreateOrder (HelloRequest) returns (HelloReply) {}
}

message HelloRequest {
  string name = 1;
}

message HelloReply {
  string message = 1;
}